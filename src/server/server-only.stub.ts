/**
 * Bản thế chỗ của gói `server-only` khi chạy test.
 *
 * Trống là đúng: cả tác dụng của `server-only` nằm ở lúc Next đóng gói, chứ
 * không phải lúc chạy. Xem phần `resolve.alias` trong `vitest.config.ts`.
 */
export {}
